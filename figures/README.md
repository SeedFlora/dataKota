# Aggregate paper figures

`reconstructed_class_errors.pdf` is a vector figure generated from the public
four-decimal class table by:

```bash
python tools/build_paper_figures.py
```

It contains reconstructed aggregate false-negative and false-positive margins,
not raw complaint records, sample-level predictions, or AI-generated imagery.
